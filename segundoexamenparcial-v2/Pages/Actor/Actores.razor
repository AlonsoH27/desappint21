@page "/Actores"
@using segundoexamenparcial_v2.Modelo;
@using segundoexamenparcial_v2.Servicio;
@inject ServicioActores serv

<h3>Información actores</h3>

<NavLink class="nav-link" href="AgregarActor">
    <span class="oi oi-plus">Agregar actores</span>
</NavLink>

@if (obj is null)
{
    <p>Cargando . . .</p>
}else {
    <table class="table">
        <thead>
            <th>Id</th>
            <th>Nombre</th>
            <th>Notas</th>
        </thead>

    <tbody>
        @foreach (var i in obj)
        {   
            <tr>
                <td>@i.Id</td>
                <td>@i.Nombre</td>
                <td>@i.Notas</td>
                <td>
                    <a class="nav-link" href="EditarActor/@i.Id">
                        <span class="oi oi-pencil">Editar</span>
                    </a>
                    <a class="nav-link" href="EliminarActor/@i.Id">
                        <span class="oi oi-pencil">Eliminar</span>
                    </a>
                </td>
            </tr>
        }
    </tbody>


    </table>
}


@code {
    List<Actor> obj;

    protected override void OnInitialized()
    {
        obj = serv.obtenerTodo("");
    }    
}